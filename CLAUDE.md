# pj-pilot — Claude 用プロジェクト指示

**運用ルール（HOW）の正本は [`AGENTS.md`](./AGENTS.md) と [`notes/`](./notes/)。**
worktree フロー / 2段ゲート / TDD / E2E / タスク管理 / デプロイ規律はそちらに従うこと。
ここには、クラウドセッション（Claude Code on the web）で特に効かせたい項目を再掲する。

> クラウドセッションではグローバル `~/.claude/CLAUDE.md` もプラグインもロードされない
> （実測で確定）。そのため必須ルールは repo-native（このリポジトリ内のファイル）で持つ。

---

## 言語

- ユーザー向けの説明・回答は **常に日本語** で書く。例外なし。
- 常に丁寧語（です・ます調）。タメ口語尾は使わない。一人称は「私」。
- 体言止め（「〜完了。」「〜を確認。」）は簡潔表現として許可。
- コミットメッセージ・コード内コメントはプロジェクトの慣習に従う。
- 技術用語・コード識別子は原語のまま。
- **調査・デバッグ作業中の短い確認コメント（「Confirmed」「Verified」等の一言）も対象。**
  作業ログ的な独り言のつもりで英語の一文を挟むミスが実際に繰り返し発生しているため、
  「これはユーザー向けの発言ではなく作業メモだから英語でもいい」という判断は行わない。
  ツール呼び出しの合間に出す一言のステータス報告も含め、ユーザーに見えるテキストは
  すべて日本語で書く。送信前に「英語の単語・文が一つでも混ざっていないか」を必ず確認する。

## 作業完了後の必須サマリー

作業が完了したら、必ず最後に以下を出力する。作業の大小を問わない。

```
---
📋 指示: <ユーザーが依頼した内容を1〜2行で要約>
✅ 完了: <実際に行った作業を箇条書き>
🎯 アクション: <ユーザーが実際に手を動かす必要がある項目>
👀 確認: <ユーザーの行動は不要だが把握しておくべき情報>
---
```

- 「アクション」と「確認」は必ず分離する。手を動かす必要があるものだけが「アクション」。
- 該当なしのときは「なし」と書く（省略しない）。
- 会話の最後に1回だけ出す（途中経過には出さない）。

## 質問の出し方

ユーザーに質問・選択肢を提示するときは **番号を振る**（回答しやすさのため）。

## 開発の標準: TDD

- 実装の前に「何をもって成功とするか」を定義し、検証手段を先に用意する。
- Red → Green → Refactor。
- **検証は自分で実行する。** 「試してください」とユーザーに丸投げしない。
  テストを書いて走らせ、PASS/FAIL を提示する。
- 計測手段そのものの信頼性を先に確かめる（古い値を返すキャッシュ等に注意）。

## E2E テスト

- ユーザー向けの主要フローには E2E テスト（Playwright）を用意し、マージ前に通す。
  単体・結合テストだけで済ませない。
- **E2E は CI 環境で動く設計にする。** 認証情報を手元の `.env.local` 前提にすると
  CI で全滅する（nozei-schedule で実際に発生）。CI 用のダミー値は `.github/ci.env` に置く。
- 標準CIは `playwright.config.*` を自動検出して `ci / e2e` ジョブを走らせる。

## コード健全性（Fallow）

- 標準CIの `quality` ジョブが未使用コード・重複・複雑度を検出する（既定 report-only）。
- 指摘でCIを落としたい場合は `.github/fallow-strict` を置く（オプトイン）。

## 可逆コストで計画の重さを決める

- 元に戻すコストが低い変更（単一ファイル・未マージ・本番未反映）は、計画を練りすぎず即実装して動かす。
- 元に戻すコストが高い変更（DBスキーマ・公開API・本番データ・マイグレーション・課金/認証）は、
  先に計画を固める。
- **スキーマ・永続データ形状の変更は、コミット前に必ず変更内容を提示して承認を取る。**

## ブランチ運用

- 機能・修正は feature ブランチで行い、**main へ直接 commit / push しない**（必ず PR 経由）。
- ブランチは `git fetch origin` → `origin/main` 起点で切る。
- 命名は `feat/<topic>` `fix/<topic>` `chore/<topic>`。
- マージ前に CI（typecheck / lint / test / build）がグリーンであることを確認する。
- マージは squash を既定とする。
- PR の状態は `gh pr list` / `gh pr view <N> --json state,mergedAt` を実行して確認する。
  記憶や伝聞で「マージした/してない」を判断しない。

## push 前のレビュー

コードを書いたら、push の前にレビューを通す。

1. `/code-review` — 差分のバグ・保守性・本番事故リスク
2. `/security-review` — ブランチ全体のセキュリティ深掘り（`origin/HEAD` が必要）

- High 以上の指摘があれば、修正を優先してから push する。
- 軽微な修正（typo・文言・変数名・コメント、1〜2行でロジック変更なし）は 1 を省略してよい。
  ロジック変更・分岐追加・外部入出力に触れる変更は対象外。
- レビュー依頼のプロンプトは「バグがない前提」ではなく **「バグがある前提で見つけて」** と
  悲観的に投げる。

### AIレビューボット指摘の裁定ポリシー

PR には複数のAIレビュー（Cursor Bugbot / Amazon Q / Devin 等）が独立に指摘を出す。
実績として誤検知がある（PR #33 では Amazon Q の Critical 5件中2件が事実誤認）。

- 指摘は鵜呑みにしない。**実際にコード・一次情報（言語仕様・フレームワークの
  ドキュメントやソース）を確認してから対応要否を判断する。**
- 複数ボットの指摘が矛盾する場合も同様に一次情報で裁定する。多数決にしない。
- 対応しない指摘には、必ず PR の当該スレッドに見送り理由を返信してから
  resolve する（ブランチ保護の会話解決必須化により、未解決のままではマージできない）。
- ボットのステータスチェック（Devin 等）は success/failure に解決しないことがあるため、
  ブランチ保護の必須チェックには含めない（必須は CI の `ci / build` / `ci / e2e` のみ）。
  チェック名は標準CI（`sinoda1114/ci-standard`）の再利用ワークフロー呼び出しに由来する
  `<呼び出し側のjob id> / <呼び出し先のjob名>` 形式。旧名（`build` / `e2e`）を指定すると
  永久に報告されず全PRがマージ不能になるため、設定前に実際のチェック一覧で確認する。

## npm / Node の既定

- `package-lock.json` / `pnpm-lock.yaml` / `yarn.lock` は **Git 管理に含める**。
  `.gitignore` に入っていたら外す提案をする。
- CI の依存解決は `npm install` ではなく **`npm ci`**
  （pnpm は `--frozen-lockfile`、yarn も同様）。
- 新規 CI には `npm audit --audit-level=high` を含める。
- `.github/dependabot.yml` がなければ作成を提案する（npm / weekly / `/`）。
- 新規プロジェクトの `.npmrc` は既定で以下を入れる。

  ```
  ignore-scripts=true
  audit-level=high
  save-exact=true
  ```

  ネイティブビルドや prepare スクリプトが必要なパッケージ（prisma / husky / puppeteer /
  sharp / better-sqlite3 等）は、その都度 `npm rebuild <pkg>` で個別に許可する。
- 不審な新規パッケージ（公開直後・メンテナ不明・ダウンロード数極小）は一度立ち止まって確認する。
  最小依存主義。

## 実装前のリサーチ

新規実装の前に、既存の実装・ライブラリを探す。

1. GitHub コード検索（`gh search repos` / `gh search code`）
2. 公式ドキュメント（Context7 / ベンダー docs）でAPI挙動とバージョン差を確認
3. パッケージレジストリ（npm / PyPI / crates.io）を確認してから自作を判断

80% を満たす既存実装があれば、それを採用・移植する方を優先する。

## 並列サブエージェント

依存関係のない実装は、サブエージェントを並列で走らせて進める。
1メッセージ内で複数の Agent 呼び出しを出すこと（逐次にしない）。

## フロントエンド変更

UI・見た目に影響する変更で PR を作るときは、変更前後のスクリーンショットを PR 説明に添付する。
見た目に影響しない変更（ロジックのみ・バックエンドのみ）では不要。

---

## クラウドで成立しないため除外した項目

以下はローカル専用のため、このテンプレートには含めていません。

| 除外項目 | 理由 |
|---|---|
| rtk（Rust Token Killer）経由のコマンド書き換え | サンドボックスにバイナリなし |
| `/ai-review`（Codex 二重レビュー） | Codex CLI 不在。gate 1 は `/code-review` で代替 |
| git worktree による並行作業の物理分離 | 使い捨てサンドボックスでは不要 |
| デプロイ実行（wrangler / vercel / npm run deploy） | 認証情報なし。PR までで止める |
| ローカル MCP（computer-use / iOS Simulator / terminal / playwright） | サンドボックスから利用不可 |
| project-bootstrap（Private テンプレートからの新規PJ作成） | Private repo 依存 |
