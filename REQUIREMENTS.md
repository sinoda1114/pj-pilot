# pj-pilot 要件メモ

社内チーム向けの総合プロジェクト管理ツール。2026-08-06 のヒアリングで確定した内容。
実装計画（`/plan`）に入る前の入力として使う。

## プロダクト概要

複数プロジェクトを横断して管理する社内向けPMツール。
Gantt/WBS はその一機能であり、課題管理・カンバン・ダッシュボードを含む。

## 技術スタック

| 領域 | 選定 | 備考 |
|---|---|---|
| フレームワーク | Next.js (App Router) | |
| ホスティング | Vercel | |
| UIキット | Mantine | DataTable/DatePicker/MultiSelect などデータ系部品が充実 |
| Ganttライブラリ | `@svar-ui/react-gantt` v2.x (MIT) | 旧 `wx-react-gantt` は GPLv3。誤用禁止 |
| DB | Turso (libSQL) | |
| ORM | Drizzle ORM | マイグレーションは drizzle-kit |
| 認証 | Google OAuth | Auth.js 想定 |

### ライブラリ選定の根拠

OSS縛り（無料）で調査した結果、SVAR React Gantt が「React純正実装・依存矢印・ドラッグ移動/リサイズ・
WBS階層・フィルター/ソート/列カスタマイズ・TypeScript・React 19対応」を無料MIT範囲で満たす唯一の選択肢。
次点は VTable-Gantt（canvas描画、カスタマイズ自由度が低い）、DHTMLX Gantt v10 Community（公式Reactラッパーが
PRO限定）。

完成品OSS（OpenProject / Plane / Vikunja 等）は全て常駐サーバ＋永続DB前提で Vercel/Cloudflare では動かないため不採用。

## 機能スコープ

### Phase 1

- 複数プロジェクト管理（PJ一覧、PJごとのGantt/課題）
- 課題管理（タスク一覧テーブル、詳細画面、ステータス、フィルター、ソート）
- WBS（階層ツリー、親子タスク、サマリータスク）
- Ganttチャート（タスクバー、タイムライン、依存矢印）
- ドラッグによる日付変更（バーの移動・リサイズ）
- 依存連動（下記「依存関係の仕様」参照）

### Phase 2

- カンバンボード
- ダッシュボード / レポート

## 依存関係の仕様

### 依存タイプ

FS（Finish-to-Start）のみ。「先行タスクが終わったら後続タスクを開始」の1種類。
ギャップ維持型の連動と組み合わせることで、バー間の隙間が実質ラグ（遅延日数）として機能するため、
明示的なラグ属性は初版では持たない。SS/FF/SF は実装しない。

### 連動方式: ギャップ維持型

ドラッグした移動量 Δ を、後続タスクチェーン全体に同じ日数だけ平行伝播する。

- 先行タスクを3日後ろにずらす → 後続チェーン全体も3日後ろへ
- 先行タスクを2日前倒し → 後続チェーン全体も2日前へ
- バー間の隙間は保たれる（詰めも広げもしない）

ClickUp / Asana / Wrike が採用する方式。CPM（クリティカルパス計算）は不要で、依存グラフを
トポロジカル順に走査して日付をシフトするだけで実装できる。

不採用にした方式:
- CPM型（MS Project / OpenProject 型）: 後続を常に「先行の終了直後」に再配置。隙間を詰める挙動が
  意図せぬ日程変更を生みやすく、実装も重い
- 警告のみ: 日付を動かさず矛盾を赤表示するだけ。要件を満たさない

### 連動のON/OFF切替

3階層すべて実装する。

1. **プロジェクト設定のトグル** — PJ単位で依存連動を有効/無効に切り替える
2. **タスク単位のピン留め** — 特定タスクを「日付固定・連動で動かさない」状態にする（締切固定タスク用）
3. **ドラッグ時の修飾キー** — Shift 等を押しながらドラッグで一時的に連動を切る。優先度は低く、Phase 1後半でも可

## タスクの属性

- タイトル
- 開始日 / 終了日
- 進捗率（%）
- 優先度
- ステータス
- **複数担当者**（1タスクに複数人アサイン可）
- **工数**: 見積時間 / 実績時間

### スキーマ上の注意

- 複数担当者のため `task_assignees` 中間テーブルが必要
- 工数カラムは初版スキーマから含める（後付けの心理的コストを避ける）
- スキーマ確定時はコミット前にユーザーへ差分を提示し承認を取る（グローバルCLAUDE.mdのルール）

## 非機能・運用

| 項目 | 決定 |
|---|---|
| 同時編集 | リアルタイム同期なし。少人数（〜5名程度）、保存時反映＋リロードで十分 |
| データ移行 | なし。新規ゼロから |
| ライセンス | OSS/無料のみ。商用ライブラリは不採用 |
| GitHubリポジトリ | **Public** |

Public 公開のため、初期セットアップ時点から以下を守る。

- シークレットをコミットしない（`.env.local` は gitignore、Turso のトークンと Google OAuth の
  クライアントシークレットは Vercel のダッシュボードが正本）
- `.npmrc` に `ignore-scripts=true` / `audit-level=high` / `save-exact=true` を置く
- lockfile はコミットする。CI は `npm ci` を使う
- LICENSE ファイルを最初に置く（依存の SVAR が MIT のため MIT が素直）

## 次のアクション

1. `/project-bootstrap` — リポジトリ立ち上げ（テンプレ展開、GitHub の type ラベル / Project / origin HEAD 設定、Vercel連携）
2. `/plan` — 実装計画（スキーマ案、画面構成、手順、リスク）

新しいセッションはこのディレクトリを cwd にして開くこと。レビュー系スキルと
プロジェクト固有 CLAUDE.md がリポジトリルートを前提にしているため。
